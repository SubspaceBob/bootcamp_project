@startuml 
title Engine Process
database input

Engine <- input : StartBtn
Engine <- input : BrakePedal

Engine -> Engine : If Engine == Off && StartBtn == 1 && BrakePedal == 100
note right of Engine : Start engine sequence is started

Engine <- GearBox : GearFunc
Engine -> Engine : Else If Engine == On && GearFunc == P (&& Velocity == 0)
note right of Engine #pink : Velocity to be checked by Gearbox? If secured remove
note right of Engine : Turn off engine sequence is started

@enduml